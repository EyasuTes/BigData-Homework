Part 1b

For tasks (1) and (3):

    javac -classpath `hadoop classpath` -d classes HadoopWordFrequency.java

    jar -cvf hadoopanalysis.jar -C classes/ .

    hadoop jar hadoopanalysis.jar HadoopWordFrequency AA output_task_1_and_3

(1) Words with count = 1000 -> Lines with prefix EXACT1000:
(3) Top-100 frequent words -> Lines with prefix TOP100:

For tasks (2) and (4):

    javac -classpath `hadoop classpath` -d classes HadoopWordPairs.java    

    jar -cvf hadoopanalysis.jar -C classes/ .

    hadoop jar hadoopanalysis.jar HadoopWordFPairs AA output_task_2_and_4


(2) Word pairs with count = 1000 -> Output where key starts with WW: and value is 1000
(4) Top-100 number-word pairs -> Filter output of HadoopWordPairs.java where key starts with NW: and post-process or add similar top-100 logic
